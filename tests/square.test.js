import Shape from "../lib/shapes.js";

describe("Shape", () => {
  describe("Square", () => {
    it("Should it be rendered square of a string", () => {
      const square = new Circle();
      square.setColor(color);
      expect(sqaure.render(Shape));
    });
  });
});

export default Square;
